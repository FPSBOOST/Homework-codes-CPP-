#include <iostream>
#include <cmath>
using namespace std;

int main () {
#define pi 3.14

    double expoente = 3;
    double raio;
    double volume;

    cout << "Insira o raio da esfera: ";
    cin >> raio;
    volume = pi * 4 * pow(raio,expoente) / 3;
    cout << volume << endl;
}
