#include <algorithm>
#include <iostream>
#include <string>
using namespace std;
int main() {
    string face = ":) :) :) smiles :)";
    std::replace(face.begin(), face.end(), ')', '(');

    cout << face << endl;

}
